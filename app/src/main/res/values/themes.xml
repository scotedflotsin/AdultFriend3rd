<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IMetLife" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->


        <!-- Customize status bar color -->
        <item name="android:statusBarColor">@color/pink</item>

    </style>






    <!-- Define the default app theme, inheriting from Base.Theme.IMetLife -->
    <style name="Theme.IMetLife" parent="Base.Theme.IMetLife" />

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Adjust soft input mode -->
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>
</resources>
